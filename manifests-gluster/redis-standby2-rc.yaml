apiVersion: v1
kind: ReplicationController
metadata:
  name: redis
  labels:
    heritage: deis
spec:
  replicas: 1
  selector:
    name: redis
  template:
    metadata:
      labels:
        name: redis
    spec:
      containers:
      - name: redis
        image: "tangfeixiong/redis:0.0.1-20160602233602"
        ports:
        - containerPort: 6379
        resources:
          limits:
            cpu: "0.1"
        volumeMounts:
        - mountPath: /redis-master-data
          # name: data
          name: standby2-gluster
          # name: standby2-claim
          # name: standby2-host
          # name: standby2-nfs
      volumes:
        - emptyDir: {}
          name: data
        - glusterfs:
            endpoints: gluster-endpoints
            path: /glustermnt/redis-standby2-data
            readOnly: false
          name: standby2-gluster
        - hostPath:
            path: /tmp/redis-standby2-data
          name: standby2-host
        - name: standby2-claim
          persistentVolumeClaim:
            claimName: standby2-pvc
        # - name: standby2-nfs
        #   nfs:
        #     path: /nfsmnt/redis-standby2-data
        #     readOnly: false
        #     server: 172.17.0.2

